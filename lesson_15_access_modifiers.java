public class lesson_15_access_modifiers {
    //the public keyword access level is everywhere
    // -- inside a class
    // -- outside a class
    // -- inside the package
    // -- outside the package

    //The private keyword access level is only within it class, 
    //it cant access methods and fields from another class,
    //it access the method and fields only within it class
 
    //The static keyword : a static non-access modifier
    // it allows you to access fields and access or call method using the class name

    public static void main(String[] args){
        
    }
}
